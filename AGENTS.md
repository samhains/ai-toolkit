# Repo Agent Notes

This repo owns ai-toolkit training/inference code, local training configs,
source LoRA datasets, and model output workflows.

Start cross-project context in:

```text
/home/excalibur/Code/project-logs-vault
```

Read:

- `AGENTS.md`
- `BASECAMP.md`
- `CURRENT_STATE.md`
- `CONVENTIONS.md`

## Notes Policy

- Write process logs, dev logs, handoffs, generation strategy, curation
  decisions, and cross-repo planning in `project-logs-vault`.
- Keep repo-local docs only when they need to travel with this code: training
  guides, config examples, implementation notes, and operational runbooks.
- Keep training outputs, checkpoints, and raw run metadata in this repo or the
  owning generation library. Summaries and decisions belong in the base-camp
  vault.
